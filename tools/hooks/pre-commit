#!/bin/sh
echo "Running pre-commit checks..."
cd "$(git rev-parse --show-toplevel)/tools" || exit 1
pnpm typecheck || { echo "Typecheck failed. Commit aborted."; exit 1; }
pnpm lint || { echo "Lint failed. Commit aborted."; exit 1; }
echo "Pre-commit checks passed."
exit 0
